# Contributing to gmux

## Task
Create a guide for developers who want to contribute to `gmux`. Source information from `Makefile`, `CLAUDE.md`, and the CI workflows.

- **Development Setup**: How to clone the repo and build the project (`make build`).
- **Running Tests**: Explain `make test` for unit tests and `make test-e2e` for end-to-end tests.
- **Code Style**: Mention `make fmt` and `make lint`.
- **Pull Request Process**: Outline the expected process for submitting changes (e.g., fork, branch, create PR).
- **CI Pipeline**: Briefly describe what the CI (`.github/workflows/ci.yml`) does (build, test).

## Output Format
Provide clean Markdown with an H1 title.