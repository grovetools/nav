# Command Reference

## Task
Create a reference for the remaining `gmux` CLI commands. For each command, explain its purpose, arguments, flags, and provide a usage example. Analyze the corresponding files in `cmd/gmux/`.

- `gmux launch`: `launch.go`
- `gmux session` (and subcommands `exists`, `kill`, `capture`): `session.go`
- `gmux status`: `main.go`
- `gmux wait`: `wait.go`
- `gmux start`: `start.go`
- `gmux version`: `version.go`

## Output Format
Provide clean Markdown with an H1 title. Use H2 for each top-level command.