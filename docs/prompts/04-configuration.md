# Configuration Reference

## Task
Create a detailed reference for the `gmux` configuration files. Analyze `internal/manager/manager.go` and `cmd/gmux/sessionize.go`.

1.  **`project-search-paths.yaml`**: Explain `search_paths`, `explicit_projects`, and `discovery` settings. Provide a full example.
2.  **`tmux-sessions.yaml`**: Explain `available_keys` and the `sessions` map.
3.  **`generated-bindings.conf`**: Explain that this file is auto-generated and should not be edited.

## Output Format
Provide clean Markdown with an H1 title: `# Configuration Reference`. Use H2 headings for each file name.