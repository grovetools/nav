# Live Sessionizer (gmux sz)

## Task
Write a comprehensive guide to the `gmux sz` TUI. Use `LIVE_SESSIONIZER.md`, `README.md`, and the `View` method in `cmd/gmux/sessionize.go` as source material.

1.  **UI Breakdown**: Explain each visual element: session indicators (`●`), key bindings, Git status summary (`↑1 ↓2 M:3...`), Claude AI status (`▶`, `⏸`), and worktree display (`└─`).
2.  **Key Bindings**: Document all interactive keys: navigation, selecting (`enter`), editing keymaps (`ctrl+e`), clearing keymaps (`ctrl+x`), copying path (`ctrl+y`), closing session (`ctrl+d`).

## Output Format
Provide clean Markdown with an H1 title. Use subheadings and tables to organize the information clearly.